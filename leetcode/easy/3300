class Solution:
    def minElement(self, nums: List[int]) -> int:
        m = float('inf')
        for num in nums:
            s = 0
            for digit in str(num):
                s += int(digit)
            m = min(m , s)
        return m
        
