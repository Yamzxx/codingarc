#Maximum sub array, return its sum 
class solution:
   def maxarray(self,arr)
        c_sum= arr[0]   #current sum
        m_sum= arr[0]   #max sum
        for j in range(1, len(arr)):
            c_sum = max(arr[i],c_sum+arr[i])
            m_sum = max(m_sum,c_sum)
         return m_sum
